import React from "react";
import Grid from "@material-ui/core/Grid";

const Form = (props) => {

	return (
		<form>
			<Grid container spacing={3}>
		    </Grid>
		</form>
	);
};

export default Form;
